#Global configuration
spring.application.name=mediClientUI
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
#Tomcat configuration
server.port=8090
## Feign URLs definitions:
feign.mediPatient.url=${MEDIPATIENT_URL:http://localhost:8081}
feign.mediNote.url=${MEDINOTE_URL:http://localhost:8082}
feign.mediAssessment.url=${MEDIASSESSMENT_URL:http://localhost:8080}
